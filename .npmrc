# Keep a flat, hoisted node_modules (like Bun/npm) so patch-package, native
# module rebuilds (better-sqlite3, node-pty, sharp) and electron-builder behave.
node-linker=hoisted
# Native deps need their install scripts to run.
enable-pre-post-scripts=true
