---
title: AX Code Server
description: Połącz AX Code App z lokalnym lub zdalnym serwerem AX Code.
---

# AX Code Server

AX Code App działa na bazie serwera AX Code. Domyślnie uruchamia go za Ciebie, więc nie musisz robić nic dodatkowego. Ta strona jest potrzebna tylko wtedy, gdy chcesz wskazać AX Code App serwer, który już uruchamiasz, albo zarządzać tym, który sam startuje.

## Jak AX Code App znajduje serwer

Gdy AX Code App się uruchamia, szuka serwera w tej kolejności:

1. ponownie używa serwera, który już uruchomił
2. łączy się z zewnętrznym, jeśli mu to wskazałeś (zobacz niżej)
3. automatycznie wykrywa serwer na domyślnym porcie (`4096`)
4. w przeciwnym razie uruchamia i obsługuje własny

Jeśli nic nie jest skonfigurowane, krok 4 dzieje się automatycznie i od razu możesz pracować.

## Połącz się z serwerem, który już uruchamiasz

Ustaw te wartości przed uruchomieniem AX Code App:

```bash
AX_CODE_HOST=http://localhost:4096 AX_CODE_SKIP_START=true ax-code-openchamber
```

- `AX_CODE_HOST` — pełny adres Twojego serwera AX Code, wraz z portem (wartość taka jak `http://localhost:4096`). Nie może zawierać ścieżki na końcu.
- `AX_CODE_SKIP_START=true` — informuje AX Code App, aby nie uruchamiał własnego serwera.

Jeśli chcesz zmienić tylko port, ustaw `AX_CODE_PORT` zamiast `AX_CODE_HOST`.

Jeśli w `AX_CODE_HOST` brakuje portu lub zawiera ścieżkę, AX Code App go zignoruje i wróci do uruchamiania własnego serwera. Obserwuj logi startowe pod kątem ostrzeżenia `[config]`, jeśli oczekiwane połączenie nie nastąpiło.

## Zarządzaj serwerem z poziomu CLI

```bash
openchamber status
openchamber logs
openchamber restart
openchamber stop
```

Samo `openchamber` uruchamia serwer w tle. Dodaj `--foreground`, aby pozostał dołączony do Twojego terminala.

## Uruchamianie AX Code App po zalogowaniu

Użyj `startup enable`, aby zainstalować natywną usługę użytkownika. AX Code App używa `launchd` na macOS, `systemd --user` na Linuksie i Harmonogramu zadań w Windows.

```bash
openchamber startup enable
openchamber startup status
openchamber startup disable
```

Aby zabezpieczyć UI, ustaw hasło podczas włączania usługi:

```bash
OPENCHAMBER_UI_PASSWORD='secret' openchamber startup enable
```

`startup enable` zapisuje migawkę bieżącego środowiska w usłudze, aby uruchomienie było bliższe ręcznemu startowi `openchamber` z tej samej powłoki. Zachowuje to tokeny dostawców, `PATH`, ustawienia agenta SSH i inne zmienne CLI auth/config. Użyj `--no-env-snapshot`, jeśli chcesz minimalne środowisko usługi.

Tunelami dla tej działającej usługi możesz zarządzać niezależnie:

```bash
openchamber tunnel start --port 3000
openchamber tunnel stop --port 3000
```

Zatrzymanie tunelu nie restartuje usługi ani aplikacji.

## „AX Code uruchamia się ponownie”

Gdy serwer startuje lub uruchamia się ponownie, AX Code App pokazuje stan „AX Code uruchamia się ponownie” i wstrzymuje żądania, aż będzie gotowy. To normalne tuż po uruchomieniu lub restarcie. Jeśli ten stan nigdy nie znika, zobacz [Połączenie z AX Code](/pl/troubleshooting/ax-code-connection/).

## Powiązane

- [Dostawcy, modele i agenci](/pl/providers/) — skonfiguruj to, z czym serwer się komunikuje
- [Połączenie z AX Code](/pl/troubleshooting/ax-code-connection/) — jeśli nie chce się połączyć
